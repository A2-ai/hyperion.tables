% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spec.R
\name{variability_rules}
\alias{variability_rules}
\title{Create variability display rules}
\usage{
variability_rules(...)
}
\arguments{
\item{...}{Formula expressions like \code{fixed ~ "(Fixed)"} or
\code{!is.na(cv) ~ sprintf("(CV = \%s\%\%)", cv)}}
}
\value{
List of quosures for use in TableSpec
}
\description{
Creates rules for constructing the \code{variability} display column. Rules are
evaluated with \code{case_when()}.
}
\examples{
variability_rules(
  fixed ~ "(Fixed)",
  !is.na(corr) ~ sprintf("(Corr = \%s)", corr),
  !is.na(cv) & cv != 0 ~ sprintf("(CV = \%s\%\%)", cv),
  !is.na(sd) ~ sprintf("(SD = \%s)", sd),
  TRUE ~ NA_character_
)
}
