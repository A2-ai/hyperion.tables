% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spec-setters.R
\name{set_spec_section_order}
\alias{set_spec_section_order}
\title{Set explicit section display order}
\usage{
set_spec_section_order(spec, ...)
}
\arguments{
\item{spec}{A TableSpec or SummarySpec object.}

\item{order}{Character vector of section labels in display order. Pass
\code{NULL} to clear.}

\item{keep_only}{If \code{TRUE}, sections not listed in \code{order} are dropped.
Defaults to \code{FALSE} (unlisted sections appear after the listed ones in
their natural encounter order).}
}
\value{
Modified spec.
}
\description{
By default, sections render in the order their rules were declared in
\code{\link[=section_rules]{section_rules()}}. Sections introduced by \code{\link[=set_spec_lookup]{set_spec_lookup()}} (i.e.
labels not present in any rule) are appended at the end. Use
\code{set_spec_section_order()} to override that with an explicit order.

Pass \code{keep_only = TRUE} to also hide any sections not listed.
Call with no arguments (or \code{order = NULL}) to clear the override.
}
\examples{
\dontrun{
  TableSpec(sections = section_rules(...)) |>
    set_spec_section_order(c(
      "Structural", "Covariate Parameters",
      "Variability", "Residual"
    ))
}
}
\seealso{
\code{\link[=get_spec_section_order]{get_spec_section_order()}}.
}
