% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spec-setters.R
\name{set_spec_lookup}
\alias{set_spec_lookup}
\title{Attach a parameter lookup TOML to a spec}
\usage{
set_spec_lookup(spec, ...)
}
\arguments{
\item{spec}{A TableSpec object.}

\item{path}{Path to a TOML file, or \code{NULL} to clear.}
}
\value{
Modified spec.
}
\description{
\code{set_spec_lookup()} registers a TOML file that supplies per-parameter
overrides used during \code{\link[=apply_table_spec]{apply_table_spec()}}. Currently the \code{section}
field is read from each entry: any parameter whose \code{name} matches a
TOML key with a \code{section = "..."} value has its section assignment
overridden after \code{\link[=section_rules]{section_rules()}} evaluation. Other fields in the TOML
are accepted (and reserved for future use) but ignored today.

Pass \code{NULL} to clear an existing lookup.
}
\examples{
\dontrun{
  TableSpec() |>
    set_spec_sections(kind == "THETA" ~ "Structural") |>
    set_spec_lookup("lookup.toml")
}
}
\seealso{
\code{\link[=get_spec_lookup]{get_spec_lookup()}}.
}
